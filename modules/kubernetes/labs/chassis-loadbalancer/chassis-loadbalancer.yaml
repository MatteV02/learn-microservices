apiVersion: v1
kind: Service
metadata:
  name: chassis-advanced-lb
spec:
  type: LoadBalancer

  # Standard service routing
  selector:
    app: product-service
  ports:
    - name: http
      port: 80
      targetPort: 8080