name: Manual Markdown to PDF converter

on:
  workflow_dispatch:
    inputs:
      folder:
        description: 'Path of the folder containing md files to convert'
        required: true
        type: string

jobs:
  md-to-pdf:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # NEW STEP: Automatically convert Mermaid code blocks to images
      # This scans your folder, renders the mermaid blocks as SVGs, and updates the markdown.
      # UPDATED STEP: Automatically convert Mermaid code blocks to images with sandbox disabled
      # UPDATED STEP: Generate Mermaid images with space-free filenames
      - name: Compile Mermaid to Images
        run: |
          npm install -g @mermaid-js/mermaid-cli
          echo '{"args": ["--no-sandbox", "--disable-setuid-sandbox"]}' > puppeteer-config.json
          
          # Ensure the images directory exists
          mkdir -p ${{ inputs.folder }}/images
          
          for file in ${{ inputs.folder }}/*.md; do
            filename=$(basename "$file")
            safename=$(echo "$filename" | tr ' ' '_')
          
            mmdc -i "$file" -o "${{ inputs.folder }}/$safename" -p puppeteer-config.json -e png -s 4
          
            if [ "$filename" != "$safename" ]; then
              mv "${{ inputs.folder }}/$safename" "$file"
            fi
          done
          
          # Move the newly generated (spaceless) Mermaid PNGs into the images folder
          mv ${{ inputs.folder }}/*.png ${{ inputs.folder }}/images/ 2>/dev/null || true
          
          # 4. Clean up the paths in the markdown (simply swap ./ for images/)
          sed -i 's|\](\./|\](images/|g' ${{ inputs.folder }}/*.md

      - name: Clear old PDF folder
        run: |
          rm -rf ${{ inputs.folder }}/pdf
          mkdir -p ${{ inputs.folder }}/pdf

      # UPDATED STEP: Generate PDF with the image_import fix
      - name: Markdown to PDF and HTML
        uses: BaileyJM02/markdown-to-pdf@v1.2.0
        with:
          input_path: ${{inputs.folder}}
          images_dir: ${{inputs.folder}}/images
          image_import: images

      # UPDATED STEP: Hunt down the PDFs and move them
      - name: Organize generated PDFs
        run: |
          echo "Hunting down generated PDFs..."

          # FIX: Target ONLY the specific folder the Docker container created
          sudo chown -R $(whoami):$(whoami) ./built

          # Move the PDFs directly from the built folder to your target folder
          mv ./built/*.pdf ${{ inputs.folder }}/pdf/

          # Clean up the entire leftover built folder (which takes care of the .html files too)
          rm -rf ./built

          echo "Verify files are in the right place:"
          ls -la ${{ inputs.folder }}/pdf/

      # Save the generated PDFs as a downloadable artifact
      - name: Commit generated PDFs
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: add generated PDFs for ${{ inputs.folder }}"
          file_pattern: ${{inputs.folder}}/pdf/*.pdf